services:
  impedance-analyzer:
    build: .
    container_name: impedance-analyzer
    restart: unless-stopped
    environment:
      DEFAULT_INSTRUMENT_IP: ${DEFAULT_INSTRUMENT_IP:-192.0.2.10}
      HOST: 0.0.0.0
      PORT: 8080
    ports:
      - "8080:8080"
