services:
  impedance-analyzer:
    build: .
    container_name: impedance-analyzer
    restart: unless-stopped
    environment:
      DEFAULT_INSTRUMENT_IP: ${DEFAULT_INSTRUMENT_IP:-10.59.133.242}
      HOST: 0.0.0.0
      PORT: 8080
      ROOT_PATH: ${ROOT_PATH:-/impedance-analyzer}
    ports:
      - "18080:8080"
